spring.application.name=Government-subsidy-Project
spring.datasource.url=jdbc:mysql://localhost:3306/infosysdb1
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=chaitanya45@
spring.jpa.hibernate.ddl-auto=update
file.upload-dir=C:\Users\krish\IdeaProjects\-Government-Subsidy-Grant-Disbursement-Tracking-System-/uploads
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Milestone 3, Task 2: gates the manual scheduler-trigger endpoints
# (/disbursement/test/run-overdue-check, /run-reminder-check).
# Set this to false (or remove the override) before deploying to
# production so those routes return 404 there.
app.dev-mode=true
